import { Injector, ComponentFactoryResolver, ApplicationRef } from '@angular/core';
import { ComponentLoader } from './component-loader';
export interface PopoverRef {
    popover: any;
    destroy: Function;
}
/**
 * @object PopoverFactory
 */
export declare class PopoverFactory {
    private _injector;
    private _resolver;
    private _appRef;
    private _componentLoader;
    constructor(_injector: Injector, _resolver: ComponentFactoryResolver, _appRef: ApplicationRef, _componentLoader: ComponentLoader);
    createPopover(componentType: any, params?: Object): Promise<PopoverRef>;
}
